/// Standard component and element sizes for the application.
class AppSizes {
  const AppSizes._();

  // ── Icon Sizes ──────────────────────────────────────────────────────────

  /// Small icons (e.g., inside small buttons or tags) - 16px
  static const double iconSm = 16.0;

  /// Medium icons (Default Material size) - 32px
  static const double iconMd = 32.0;

  /// Large icons (e.g., for headers or feature highlights) - 48px
  static const double iconLg = 48.0;

  /// Extra large icons - 80px
  static const double iconXl = 80.0;

  // ── Button Heights ──────────────────────────────────────────────────────

  /// Small buttons - 32px
  static const double buttonSm = 32.0;

  /// Standard buttons - 48px
  static const double buttonMd = 48.0;

  /// Large buttons - 56px
  static const double buttonLg = 56.0;

  // ── Common UI Elements ──────────────────────────────────────────────────

  /// App bar height - 56px
  static const double appBarHeight = 56.0;

  /// Side navigation rail width (web/desktop layout) - 248px
  static const double sideNavWidth = 248.0;
}
